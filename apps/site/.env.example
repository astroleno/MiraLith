# Server-only local review switch. Never rename this to NEXT_PUBLIC_*.
# "off" is the only production-safe value; "local-preview" is for `next dev`.
MIRALITH_POST_COSCROLL_MEDIA_MODE=off

# Optional local binary overrides used only by the preparation scripts.
# FFMPEG_PATH=/opt/homebrew/bin/ffmpeg
# FFPROBE_PATH=/opt/homebrew/bin/ffprobe
