# LuBirth opening cloud asset evidence

## Result

- **Cloud-asset contract: PASS.** `desktop.mp4` and `mobile.mp4` are only packed RGB + alpha cloud media; no Earth, Moon, stars, sky, copy or UI is present in the payload.
- **Default-home promotion: REJECT / not eligible.** This branch remains query-only. The automated browser cannot expose `EXT_disjoint_timer_query_webgl2`, so it cannot certify the required physical-device post-handoff Relief-lite GPU p95 ≤3 ms gate. No default route was changed.

## Asset facts

- Source: deterministic internal procedural WebGL2 volume, fixed seed and internal license; `manifest.json` records source/output SHA-256.
- Timeline: 48 all-I frames at 30 fps; the authored 1.584 s timeline maps to opening progress 0–0.195.
- Handoff: the cloud layer ends under an independent shared veil; the real IP Relief-lite Canvas remains continuously behind it.
- Desktop: 1,440×810 raw / 2,880×810 packed, 3,023,054 bytes, 4,665,600-byte presentation estimate.
- Mobile: 960×444 raw / 1,920×444 packed, 1,194,458 bytes, 1,704,960-byte presentation estimate.
- `desktop-cloud-contact-sheet.png` and `mobile-cloud-contact-sheet.png` show frames 0, 12, 24, 36, 39 and 42 over an evidence-only dark matte.

## Runtime proof

`runtime-desktop-contact-sheet.png` is captured from the actual `/lubirth-cloud-asset-opening` route at frames 0, 26 and 39. The Earth and Moon are drawn by the live IP Canvas; only the cloud layer changes. The route test uses native page scrolling, verifies frame 0→26 motion, proves the independent veil is above both layers, verifies the atomic handoff, release at ≥0.22, and reverse re-entry.

`runtime-telemetry.json` records the required cold-start, target-frame, rAF, dropped-frame and decode-error telemetry from a non-promotable local HeadlessChromium run. GPU timing is explicitly marked unsupported rather than treated as a pass.

The actual payload is transparent cloud media; it contains no baked Earth, moon, sky, copy, or UI.
